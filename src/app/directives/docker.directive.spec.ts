import { DockerDirective } from './docker.directive';

describe('DockerDirective', () => {
  it('should create an instance', () => {
    const directive = new DockerDirective();
    expect(directive).toBeTruthy();
  });
});
