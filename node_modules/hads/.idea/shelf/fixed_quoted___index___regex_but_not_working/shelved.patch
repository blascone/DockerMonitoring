Index: lib/renderer.js
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
--- lib/renderer.js	(date 1474989799000)
+++ lib/renderer.js	(revision )
@@ -11,6 +11,9 @@
 const SEARCH_EXTRACT_LENGTH = 400;
 const SEARCH_RESULTS_MAX = 10;
 
+// Match [[index]] except between ``, including escaped ``
+const INDEX_REGEX = /\\`|`(?:\\`|[^`])*`|(\[\[index]])/g;
+
 class Renderer {
 
   constructor(indexer) {
@@ -24,7 +27,7 @@
 
   renderFile(file) {
     return this.renderRaw(file).then((content) => {
-      content = content.replace(/\[\[index]]/g, this.renderIndex());
+      content = content.replace(INDEX_REGEX, this.renderIndex());
       return Renderer.renderMarkdown(content);
     });
   }
